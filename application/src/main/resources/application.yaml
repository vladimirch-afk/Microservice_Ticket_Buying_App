spring:
  application:
    name: "application"
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://app-db:5432/
    username: postgres
    password: postgres
server:
  port: 8081
